Steps for setting up the server: 

(1). Install required python modules using pip. 

			pip install -r requirements.txt --user

(2). Apply migrations

			python manage.py migrate
		
(3). Start servers

			python manage.py runserver
		
		
